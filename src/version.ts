/**
 * Application version (SemVer).
 *
 * Single source of truth for the version number. Bump it here (and in
 * deno.json / git tags) for each release; the header surfaces it in the UI.
 */
export const APP_VERSION = "0.1.0";